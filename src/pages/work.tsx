import * as React from 'react'
import Layout from '../components/layout'

const work_page = () => {
    return (
        <Layout pageTitle="Work Page">
            <p>testing my ability to create a page component</p>
        </Layout>
    )
}

export const Head = () => <title>About Me</title>
export default work_page
